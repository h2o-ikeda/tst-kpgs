<!DOCTYPE html>
<html>
  <head>
    <title>申請情報詳細画面</title>
    <link type="text/css" rel="stylesheet" href="{{ url_for('static', filename='sample.css') }}">
  </head>
  <body>
    <div class="content">
      <div class="contentposition">
        <p>申請ID：{{requestID}}</p>
        <p>申請日：{{requestDate}}</p>
        <p>申請者名：{{requestName}}</p>
        <p>申請者メールアドレス：{{requestAddress}}</p>
        <p>タイトル：{{title}}</p>
        <p>No.{{number}}</p>
        <p>ポケモン名：{{pokemonName}}</p>
        <p>タイプ：{{type}}</p>
        <p>紹介文：{{introduction}}</p>
        <p>HP：{{hp}}</p>
        <p>こうげき：{{attack}}</p>
        <p>ぼうぎょ：{{defense}}</p>
        <p>とくこう：{{special_attack}}</p>
        <p>とくぼう：{{special_defense}}</p>
        <p>すばやさ：{{quickness}}</p>
        <p>ステータス：{{status}}</p>
        <p>承認者①名前：{{approvalName_1}}</p>
        <p>承認者①メールアドレス：{{approvalAddress_1}}</p>
        <p>承認者②名前：{{approvalName_2}}</p>
        <p>承認者②メールアドレス：{{approvalAddress_2}}</p>
      </div>
      <form method="POST" id="myForm" action="" name="inputForm">
       <input type="hidden" name="search_type" value="{{search_type}}">
       <input type="hidden" name="number" value="{{number}}">
       <input type="hidden" name="pokemonName" value="{{pokemonName}}">
       <input type="hidden" name="type" value="{{type}}">
       <input type="hidden" name="intro" value="{{intro}}">
       <input type="hidden" name="hp" value="{{hp}}">
       <input type="hidden" name="attack" value="{{attack}}">
       <input type="hidden" name="defense" value="{{defense}}">
       <input type="hidden" name="special_attack" value="{{special_attack}}">
       <input type="hidden" name="special_defense" value="{{special_defense}}">
       <input type="hidden" name="quickness" value="{{quickness}}">
      {% if {{status}}=="承認者①承認待ち" or {{status}}=="承認者②承認待ち" %}
      <button type="button" class="btn" onclick="updatepage()">承認</button><br>
      {% endif %}
      <button type="button" class="btn" id="returnbutton" onclick="listpage()">申請情報一覧画面に戻る</button>
      </form>
    </div>
  </body>
  <script>
    function updatepage() {
    document.getElementById("myForm").action = "/update";
    document.getElementById("myForm").submit();
　　} 
　　
    function listpage() {
    document.getElementById("myForm").action = "/";
    document.getElementById("myForm").submit();
  }
  </script>
</html>
